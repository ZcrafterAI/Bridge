[project]
name = "mainframe-mcp"
version = "0.1.0"
description = "MCP server exposing typed mainframe/z/OS tools over Streamable HTTP for the ZCrafter backend"
readme = "README.md"
requires-python = ">=3.11"
license = { file = "LICENSE" }

[tool.pytest.ini_options]
testpaths = ["tests"]
